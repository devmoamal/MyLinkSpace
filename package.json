{
  "name": "mylinkspace",
  "private": true,
  "scripts": {
    "install:env": "cp backend/.env.example backend/.env",
    "install:all": "bun install --cwd backend && bun install --cwd frontend && bun run install:env",
    "dev:backend": "cd backend && bun dev index.ts",
    "dev:frontend": "cd frontend && bun dev",
    "dev": "bun run dev:backend & bun run dev:frontend"
  }
}
